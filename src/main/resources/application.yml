spring:
  profiles:
    active: dev
  servlet:
    multipart:
      max-request-size: 50MB

server:
  servlet:
    context-path: /api/

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
  default-produces-media-type: application/json
